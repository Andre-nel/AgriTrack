# Legacy Web

Temporary compatibility module for the original mixed web blueprint.

This module keeps current Flask `web.*` endpoints stable by assembling
domain-owned route registrations from `dashboard`, `analytics`, `imports`,
`farms`, `paddocks`, `mobs`, `grazing`, and `water`.
