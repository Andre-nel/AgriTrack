[pytest]
addopts =
    -p no:cacheprovider
testpaths = tests
markers =
    unit: fast service/model tests
    integration: Flask route and workflow tests
norecursedirs =
    .git
    .venv
    venv
    .pytest_cache
    .pytest-tmp
    pytest_tmp
    pytest-cache-files-*
